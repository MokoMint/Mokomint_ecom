name: Deploy to Azure App Service

on:
  push:
    branches:
      - main
      - 'release/*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      artifact-name: ${{ steps.set-artifact.outputs.artifact-name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'next-app/package-lock.json'

      - name: Install dependencies
        working-directory: './next-app'
        run: npm ci

      - name: Build Next.js app
        working-directory: './next-app'
        run: npm run build

      - name: Package deployment artifact
        working-directory: './next-app'
        run: |
          rm -f ../deployment-package.zip
          zip -r ../deployment-package.zip . -x "node_modules/*" ".git/*"

      - name: Set artifact name
        id: set-artifact
        run: echo "artifact-name=app-service-${{ github.run_id }}" >> $GITHUB_OUTPUT

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.set-artifact.outputs.artifact-name }}
          path: deployment-package.zip
          if-no-files-found: error
          retention-days: 60

  approval:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: Production

    steps:
      - name: Manual Approval Confirmed
        run: echo "✅ Deployment approved and proceeding to Azure App Service"

  deploy:
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/')
    runs-on: ubuntu-latest
    needs: [build, approval]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.artifact-name }}
          path: ./artifact

      - name: Debug - Check secrets
        run: |
          echo "Client ID: ${{ secrets.AZURE_CLIENT_ID }}"
          echo "Tenant ID: ${{ secrets.AZURE_TENANT_ID }}"
          echo "Subscription ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}"

      - name: Azure CLI Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure App Service
        run: |
          az webapp deploy \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --name ${{ secrets.AZURE_WEBAPP_NAME }} \
            --src-path ./artifact/deployment-package.zip \
            --type zip

      - name: Logout from Azure
        run: az logout

      - name: Deployment Summary
        if: success()
        run: |
          echo "========================================"
          echo "✅ DEPLOYMENT SUCCESSFUL"
          echo "========================================="
          echo "Web App: ${{ secrets.AZURE_WEBAPP_NAME }}"
          echo "Resource Group: ${{ secrets.AZURE_RESOURCE_GROUP }}"
          echo "Deployment ID: ${{ github.run_id }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "========================================="
